/* const pokmon = pokemonList.map((pokemon, index) => (
  <figure key={index}>
  {pokemon.imgSrc ? (
    <>
    <img src = {pokemon.imgSrc} alt ={pokemon.name}/>
    <figcaption>{pokemon.name}<figcaption/>
  ) : (
     <p>{pokemon.imgSrc = '???'} alt = {pokemon.name}</p>
      <figcaption>{pokemon.name}<figcaption/>
    </>
  )}
  </figure>
))*/ 